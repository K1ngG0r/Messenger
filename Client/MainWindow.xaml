<Window x:Class="Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client.Views"
        xmlns:vm="clr-namespace:Client.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Background="AliceBlue">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" Name="column1"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>

        <ContentControl Grid.Column="0" Content="{Binding ActivePageViewModel}">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type vm:MainPageViewModel}">
                    <local:MainPage/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:SettingsPageViewModel}">
                    <local:SettingsPage/>
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>
        <ContentControl Grid.Column="1" Content="{Binding ChatPageViewModel}">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type vm:ChatPageViewModel}">
                    <local:ChatPage/>
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>

        <!--<Thumb Grid.Column="1" Grid.ColumnSpan="2" Background="Gray" Cursor="SizeWE"
               Width="1" DragDelta="Thumb_DragDelta" HorizontalAlignment="Left"/>-->
    </Grid>
</Window>
