<UserControl x:Class="Client.Views.MainPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="250">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0">
            <Button Content="..." Width="50" FontSize="30" DockPanel.Dock="Left"/>
            <TextBox Text="{Binding SearchText, Mode=TwoWay}" TextAlignment="Left" Padding="5"/>
        </DockPanel>
        <ListBox ItemsSource="{Binding ChatsList}" SelectedItem="{Binding SelectedChat, Mode=TwoWay}" Grid.Row="1">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border CornerRadius="10" Background="LightBlue">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="50" 
                                    Height="50" 
                                    CornerRadius="25"
                                    ClipToBounds="True"
                                    Margin="5"
                                    Background="Gray">
                                <Image Source="{Binding ImagePath}" 
                                       Stretch="UniformToFill">
                                    <Image.Clip>
                                        <EllipseGeometry Center="25,25" RadiusX="25" RadiusY="25"/>
                                    </Image.Clip>
                                </Image>
                            </Border>
                            <TextBlock Margin="10" Text="{Binding ChatName}" FontSize="16"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
