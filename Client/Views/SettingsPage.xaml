<UserControl x:Class="Client.Views.SettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="250">
    <Grid>
        <Grid.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="FontSize" Value="20"/>
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="Margin" Value="5"/>
            </Style>
            <Style TargetType="Button">
                <Setter Property="Margin" Value="5"/>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Settings" VerticalAlignment="Center" TextAlignment="Center" FontWeight="Bold"/>
            <Button Grid.Column="1"
                Content="..." Width="50" 
                    HorizontalAlignment="Right"
                FontSize="30" 
                Command="{Binding NavigateToMainPageCommand}"/>
        </Grid>
        <local:AvatarImage Grid.Row="1" DataContext="{Binding Avatar}"/>
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Name"/>
            <TextBox Grid.Row="1" Text="{Binding Name, Mode=TwoWay}"/>
        </Grid>
        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Username"/>
            <TextBox Grid.Row="1" Text="{Binding Username, Mode=TwoWay}"/>
        </Grid>
        <Grid Grid.Row="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="AvatarPath"/>
            <TextBox Grid.Row="1" Text="{Binding AvatarPath, Mode=TwoWay}"/>
            <Button Grid.Row="1" Grid.Column="1"
                    Content="Select image"
                    Command="{Binding SelectImageCommand}"/>
        </Grid>
        <Button Grid.Row="5" Content="Submit"
                Command="{Binding SubmitCommand}"
                IsEnabled="{Binding CanSubmit}"/>
        <Button Grid.Row="6" Content="Logout"
                Command="{Binding LogoutCommand}"/>
    </Grid>
</UserControl>
