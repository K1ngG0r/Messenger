<UserControl x:Class="Client.Views.AvatarImage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Client.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="100">
    <Grid>
        <Border Grid.Row="0"
                Width="{Binding SideLength}" 
                Height="{Binding SideLength}" 
                CornerRadius="{Binding Radius}"
                ClipToBounds="True"
                Margin="5"
                Background="{Binding BackgroundChatImage}">
            <Image Source="{Binding ImagePath}" 
                   Stretch="UniformToFill">
                <Image.Clip>
                    <EllipseGeometry Center="{Binding EllipseCenter}"
                                     RadiusX="{Binding Radius}"
                                     RadiusY="{Binding Radius}"/>
                </Image.Clip>
            </Image>
        </Border>
    </Grid>
</UserControl>
